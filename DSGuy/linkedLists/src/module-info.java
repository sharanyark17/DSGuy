module linkedLists {
	
	/* note : this exports the packages
	 specify these package names in the new project folder where it is required
	 add this (outermost whole) project name in properties --> project --> module --> add  
	import the package.classname in the class where it is needed
	*/
	
	exports linkedLists;
	exports node;
}